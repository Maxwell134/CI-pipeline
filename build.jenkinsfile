library identifier: "pipeline@main", retriever: modernSCM (
    [$class: "GitSCMSource",  
     remote: "https://github.com/Maxwell134/shared_libs.git"])

shared_libs {
    DOCKER_IMAGE = "myapp3"
    DOCKER_TAG = "${env.GIT_COMMIT}"
}


